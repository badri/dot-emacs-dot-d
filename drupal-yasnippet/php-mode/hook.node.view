# -*- mode: snippet -*-
# name: Hook Node View
# key: hook_node_view
# --
/**
 * Implements hook_node_view().
 *
 * TODO (`(getenv "USER")`): Add documentation.
 */
function `(file-name-nondirectory (file-name-sans-extension (buffer-file-name)))`_node_view($node, $view_mode, $langcode) {
$0
}