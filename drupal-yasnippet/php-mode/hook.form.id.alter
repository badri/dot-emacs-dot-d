# -*- mode: snippet -*-
# name: Hook Form ID Alter
# key: hook_form_id_alter
# --
/**
 * Implements hook_form_FORM_ID_alter().
 *
 * TODO (`(getenv "USER")`): Add method documentation explaining why we hook.
 */
function `(file-name-nondirectory (file-name-sans-extension (buffer-file-name)))`_form_${1:FORM_ID}_alter(&$form, &$form_state, $form_id) {
  $0
  return $form;
}